import React from 'react'
import { ICCheckboxProps } from '../../types/UI-Kit/checkbox/ICCheckbox'

const CCheckbox = ({ onClick, text, value, error, disabled }: ICCheckboxProps): JSX.Element => {
  return (
    <>
      <input type="checkbox" />
    </>
  )
}

export default CCheckbox
